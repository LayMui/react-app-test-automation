resources: 
  pipelines:
  - pipeline: 1
    project: mui_ume
    source: LayMui.react-app-typescript
    branch: master  
    trigger:  
      enabled: true
 
pool:
  vmImage: 'ubuntu-latest'
 
steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'
