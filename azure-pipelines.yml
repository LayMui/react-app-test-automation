resources: 
  pipelines:
  - pipeline: 10
    project: YAML
    source: Testaaabbb
    branch: master  
    trigger:  
      enabled: true
 
pool:
  vmImage: 'ubuntu-latest'
 
steps:
- script: echo Hello, world!
  displayName: 'Run a one-line script'
